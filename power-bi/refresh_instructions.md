# Refresh Instructions

Regenerate modeled outputs and dashboard inputs:

```bash
python scripts/run_all.py
python scripts/run_sql.py
```

Open the manually built `.pbix`, confirm the CSV paths point to `data/powerbi/`, and use Home -> Refresh.
